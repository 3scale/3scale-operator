apiVersion: api.3scale.net/v1alpha1
kind: Binding
metadata:
  name: test
  labels:
     environment: production
spec:
  credentialsRef:
    name: mysecret
  APISelector:
    matchLabels:
       environment: production
---
apiVersion: api.3scale.net/v1alpha1
kind: Binding
metadata:
  name: empty-selector
spec:
  credentialsRef:
    name: mysecret
  APISelector:
    matchLabels:
       environment: empty

