---
version: 2.1

jobs:
  noop:
    docker:
      - image: alpine:latest
    steps:
      - run: echo "noop"

workflows:
  version: 2
  noop:
    when:
      not:
        equal: [ master, << pipeline.git.branch >> ]
    jobs:
      - noop
