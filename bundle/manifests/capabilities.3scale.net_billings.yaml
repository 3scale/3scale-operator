---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: billings.capabilities.3scale.net
spec:
  group: capabilities.3scale.net
  names:
    kind: Billing
    listKind: BillingList
    plural: billings
    singular: billing
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: Billing is the Schema for the Billings API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: BillingSpec defines the desired state of Billing
            properties:
              accountID:
                description: Tenant Account ID
                format: int64
                type: integer
              billingConfig:
                description: BillingConfig describes how to configure Billing process
                properties:
                  billAllTenants:
                    description: Bill All Tenants, Defaults to "true"
                    type: boolean
                  billDayOfMonth:
                    description: Bill Day of Month, Defaults - 1st day of the month
                    type: integer
                  billNow:
                    description: Bill in next cycle, Defaults - false. After the bill
                      - set to false
                    type: boolean
                type: object
              developerAccountID:
                description: ID of the developer account
                format: int64
                type: integer
              providerAccountRef:
                description: ProviderAccountRef references account provider credentials
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - billingConfig
            type: object
          status:
            description: BillingStatus defines the observed state of Billing
            properties:
              accountID:
                description: Tenant Account ID
                format: int64
                type: integer
              billDate:
                type: string
              billingId:
                format: int64
                type: integer
              conditions:
                description: Current state of the Billing. Conditions represent the
                  latest available observations of an object's state
                items:
                  description: "Condition represents an observation of an object's state. Conditions are an extension mechanism intended to be used when the details of an observation are not a priori known or would not apply to all instances of a given Kind. \n Conditions should be added to explicitly convey properties that users and components care about rather than requiring those properties to be inferred from other observations. Once defined, the meaning of a Condition can not be changed arbitrarily - it becomes part of the API, and has the same backwards- and forwards-compatibility concerns of any other part of the API."
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      description: ConditionReason is intended to be a one-word, CamelCase
                        representation of the category of cause of the current status.
                        It is intended to be used in concise output, such as one-line
                        kubectl get output, and in summarizing occurrences of causes.
                      type: string
                    status:
                      type: string
                    type:
                      description: "ConditionType is the type of the condition and is typically a CamelCased word or short phrase. \n Condition types should indicate state in the \"abnormal-true\" polarity. For example, if the condition indicates when a policy is invalid, the \"is valid\" case is probably the norm, so the condition should be called \"Invalid\"."
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              developerAccountID:
                description: ID of the developer account
                format: int64
                type: integer
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed Billing Spec.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null